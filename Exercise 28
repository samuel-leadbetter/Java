// 28. Write a Java program to convert a hexadecimal to a decimal number.

import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        System.out.println("Input a hexadecimal number to convert to decimal: ");
        String hexNum = input.next();
        int decNum = Integer.parseInt(hexNum,16);
        System.out.println(hexNum + " to decimal is: " + decNum);
    }
}
